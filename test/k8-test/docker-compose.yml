version: '3'
services:
  k8-test:
    container_name: k8-test
    restart: always
    build: ./.
    ports: 
    - "80:80"
  #nginx:
  #  container_name: nginx
  #  image: nginx:1.14-alpine
  #  volumes:
  #  - ./nginx:/etc/nginx/conf.d
  #  ports:
  #  - "80:80"
